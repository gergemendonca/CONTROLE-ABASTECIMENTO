import {env} from 'cloudflare:workers';
import {requireRole} from '../admin-auth';
type Item={tripId:number;liters:number;routeKm:number;paymentStatus:'total'|'parcial'|'nao_pago';outstandingCents:number};
const paymentOk=(p:string)=>p==='total'||p==='parcial'||p==='nao_pago';
async function notify(userIds:number[],title:string,message:string,href:string){const ids=[...new Set(userIds)].filter(Number.isSafeInteger);if(ids.length)await env.DB!.batch(ids.map(userId=>env.DB!.prepare('INSERT INTO app_notifications (user_id,title,message,href,created_at) VALUES (?,?,?,?,?)').bind(userId,title,message,href,new Date().toISOString())))}
export async function POST(req:Request){const access=await requireRole(req,'solicitante');if(!access.user)return Response.json({error:access.error},{status:403});try{const {requests}=await req.json() as {requests?:Item[]};if(!Array.isArray(requests)||requests.length===0)return Response.json({error:'Selecione ao menos uma viagem.'},{status:400});for(const item of requests){if(!Number.isSafeInteger(item.tripId)||!Number.isFinite(item.liters)||item.liters<=0||!Number.isSafeInteger(item.routeKm)||item.routeKm<0||!paymentOk(item.paymentStatus)||!Number.isSafeInteger(item.outstandingCents)||item.outstandingCents<0||(item.paymentStatus!=='total'&&item.outstandingCents<=0))return Response.json({error:'Confira litros, KM e pagamento de cada viagem.'},{status:400})}const now=new Date().toISOString();const created:number[]=[];for(const item of requests){const existing=await env.DB!.prepare("SELECT id FROM fuel_requests WHERE trip_id=? AND status='pending' LIMIT 1").bind(item.tripId).first();if(existing)return Response.json({error:'Esta viagem já possui um pedido pendente de autorização.'},{status:409});const row=await env.DB!.prepare('INSERT INTO fuel_requests (trip_id,requested_by,liters,route_km,payment_status,outstanding_cents,status,created_at) VALUES (?,?,?,?,?,?,\'pending\',?) RETURNING id').bind(item.tripId,access.user.bootstrap?null:access.user.id,item.liters,item.routeKm,item.paymentStatus,item.outstandingCents,now).first<{id:number}>();if(row)created.push(row.id);const associated=await env.DB!.prepare('SELECT user_id AS id FROM trip_users WHERE trip_id=?').bind(item.tripId).all<{id:number}>();const authorizers=await env.DB!.prepare("SELECT id FROM app_users WHERE active=1 AND (roles LIKE '%\"autorizador\"%' OR roles LIKE '%\"admin\"%')").all<{id:number}>();await notify([...associated.results.map(x=>x.id),...authorizers.results.map(x=>x.id)],'Novo pedido de abastecimento','Há um pedido pendente de autorização.',`/autorizacoes?pedido=${row?.id}`)}return Response.json({ids:created});}catch{return Response.json({error:'Não foi possível criar o pedido de abastecimento.'},{status:503})}}
export async function GET(req:Request){const access=await requireRole(req,'solicitante','autorizador','avisado');if(!access.user)return Response.json({error:access.error},{status:403});const params=new URL(req.url).searchParams;const requestedStatus=params.get('status');const status=requestedStatus==='pending'||requestedStatus==='authorized'?requestedStatus:null;const admin=access.user.roles.includes('admin'),authorizer=access.user.roles.includes('autorizador');const userId=access.user.id;let where=status?" WHERE f.status=?":'';const binds:unknown[]=status?[status]:[];if(!admin&&!authorizer){where+=(where?' AND ':' WHERE ')+"EXISTS (SELECT 1 FROM trip_users tu WHERE tu.trip_id=f.trip_id AND tu.user_id=?)";binds.push(userId)}const q=`SELECT f.id,f.trip_id AS tripId,f.liters,f.route_km AS routeKm,f.payment_status AS paymentStatus,f.outstanding_cents AS outstandingCents,f.status,f.created_at AS createdAt,f.authorized_at AS authorizedAt,v.label AS vehicleLabel,t.route,t.departure_date AS departureDate,t.arrival_date AS arrivalDate,COALESCE(u.name,'Administrador') AS requestedBy,COALESCE(a.name,'Administrador') AS authorizedBy FROM fuel_requests f JOIN trips t ON t.id=f.trip_id JOIN vehicles v ON v.id=t.vehicle_id LEFT JOIN app_users u ON u.id=f.requested_by LEFT JOIN app_users a ON a.id=f.authorized_by${where} ORDER BY CASE f.status WHEN 'pending' THEN 0 ELSE 1 END,f.created_at DESC`;const rows=await env.DB!.prepare(q).bind(...binds).all();return Response.json({requests:rows.results});}
