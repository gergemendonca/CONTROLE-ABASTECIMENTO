'use client';
import {useState} from 'react';
import AdminClient from './admin-client';
export default function Admin(){const [allowed,setAllowed]=useState(false),[password,setPassword]=useState(''),[error,setError]=useState('');async function enter(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin-session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});if(r.ok){setPassword('');setAllowed(true)}else setError('Senha incorreta.')}if(allowed)return <AdminClient/>;return <main className="min-h-screen bg-slate-50 p-5"><section className="mx-auto mt-16 max-w-md rounded-3xl bg-white p-7 shadow"><h1 className="text-3xl font-bold">Área administrativa</h1><p className="mt-2 text-lg">Informe a senha para acessar as viagens.</p><form onSubmit={enter} className="mt-6 space-y-4"><input autoFocus type="password" inputMode="numeric" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" className="h-16 w-full rounded-xl border p-4 text-2xl"/><button className="h-14 w-full rounded-xl bg-[#096a9b] text-xl font-bold text-white">Entrar</button></form>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-lg text-red-800">{error}</p>}</section></main>}
