'use client';
import {useEffect,useState} from 'react';
import AdminClient from './admin-client';
type Session={name:string;roles:string[]};
export default function Admin(){const [user,setUser]=useState<Session|null>(null),[checking,setChecking]=useState(true),[username,setUsername]=useState('admin'),[password,setPassword]=useState(''),[error,setError]=useState('');const params=typeof window==='undefined'?null:new URLSearchParams(window.location.search),next=params?.get('next')||'',force=params?.get('force')==='1',destination=next.startsWith('/')?next:(force?'/admin':'/');useEffect(()=>{void fetch('/api/admin-session',{cache:'no-store'}).then(r=>r.json()).then(x=>setUser(x.user||null)).finally(()=>setChecking(false))},[]);useEffect(()=>{if(!force&&user&&!user.roles.includes('admin'))window.location.replace(destination)},[user,destination,force]);async function enter(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin-session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,password})});const x:any=await r.json();if(r.ok){setPassword('');window.location.assign(destination)}else setError(x.error||'Usuário ou senha incorretos.')}if(checking)return <main className="grid min-h-screen place-items-center bg-slate-50 text-lg">Verificando acesso...</main>;if(!force&&user?.roles.includes('admin'))return <AdminClient/>;if(!force&&user)return <main className="grid min-h-screen place-items-center bg-slate-50 text-lg">Abrindo seu painel...</main>;return <main className="min-h-screen bg-slate-50 p-5"><section className="mx-auto mt-16 max-w-md rounded-3xl bg-white p-7 shadow"><h1 className="text-3xl font-bold">Área administrativa</h1><p className="mt-2 text-lg">Informe usuário e senha para acessar o Adm.</p><form onSubmit={enter} className="mt-6 space-y-4"><input autoFocus autoCapitalize="none" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Usuário" className="h-16 w-full rounded-xl border p-4 text-xl"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" className="h-16 w-full rounded-xl border p-4 text-2xl"/><button className="h-14 w-full rounded-xl bg-[#096a9b] text-xl font-bold text-white">Entrar</button></form>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-lg text-red-800">{error}</p>}</section></main>}
